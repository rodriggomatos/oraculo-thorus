"""Knowledge graph leve — `entities` + `relations`.

Existe desde a Fase 1 (mesmo populado só com projetos) pra que a estrutura
cresça sem reescrita. Conectores escrevem aqui durante a normalização;
agentes consultam pra raciocínio relacional.
"""
